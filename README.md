# Alerty — public download site

This repo hosts:
- The public landing/download page (deployed via GitHub Pages)
- Published release installers (`.dmg` / `.exe`), auto-uploaded by CI from the private application source repo

The app's actual source code lives in a separate private repository.
